{% extends 'base.html.twig' %}

{% block title %}{{ tag.label }}{% endblock %}

{% block body %}

    <h1> Balise : {{ tag.label }}</h1>
    <p>
        Article(s) :
    </p>
    <ul>
        {% for article in tag.articles  %}
            {% include 'article/_thumbnail.html.twig' with { 'article': article } %}
        {% endfor %}
    </ul>

{% endblock %}
